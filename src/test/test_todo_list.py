import unittest


class Reminder(unittest.TestCase):
    def test_roadmap(self):
        """Combine the steps from the evaluation service and the framed
        mapper into one pipeline so it's easy to see what's happening to the request start to finish"""
        assert False
